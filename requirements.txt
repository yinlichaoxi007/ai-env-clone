# Dependency for building the executable.
# PyInstaller bundles the GUI app and the ai_env_clone package into a single file.
# Run `python -m pip install -r requirements.txt` before `python build_exe.py`.
PyInstaller>=6.0.0
